import HomePage from "./pages/HomePage";

/**
 * App — application root. Add routing here (e.g. react-router-dom) as the
 * project grows into a multi-page experience.
 */
export default function App() {
  return <HomePage />;
}
